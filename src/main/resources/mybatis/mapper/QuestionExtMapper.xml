<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.community.mapper.QuestionExtMapper">
  <resultMap id="BaseResultMap" type="com.example.community.model.Question">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Thu May 21 19:49:09 CST 2020.
    -->
    <id column="ID" jdbcType="INTEGER" property="id" />
    <result column="TITLE" jdbcType="VARCHAR" property="title" />
    <result column="GET_CREATE" jdbcType="BIGINT" property="getCreate" />
    <result column="GET_MODIFIED" jdbcType="BIGINT" property="getModified" />
    <result column="CREATOR" jdbcType="VARCHAR" property="creator" />
    <result column="COMMENT_COUNT" jdbcType="INTEGER" property="commentCount" />
    <result column="VIEW_COUNT" jdbcType="INTEGER" property="viewCount" />
    <result column="LIKE_COUNT" jdbcType="INTEGER" property="likeCount" />
    <result column="TAG" jdbcType="VARCHAR" property="tag" />
    <result column="NEW_VIEW" jdbcType="INTEGER" property="newView" />
  </resultMap>

  <update id="incView" parameterType="com.example.community.model.Question">
    update QUESTION
    set VIEW_COUNT = VIEW_COUNT + #{viewCount,jdbcType=INTEGER}
    where id = #{id}
  </update>
  <update id="incCommentCount" parameterType="com.example.community.model.Question">
    update QUESTION
    set COMMENT_COUNT = COMMENT_COUNT + #{commentCount,jdbcType=INTEGER}
    where id = #{id}
  </update>
  <select id="selectRelated" parameterType="com.example.community.model.Question" resultMap="BaseResultMap">
    select * from QUESTION where id != #{id} and tag regexp #{tag}
  </select>
  <select id="countBySearch" parameterType="com.example.community.dto.QuestionQueryDTO" resultType="java.lang.Integer">
    select count(*) from QUESTION
    <where>
      <if test="search != null">
        and title regexp #{search}
      </if>
    </where>
  </select>
  <select id="selectBySearch" parameterType="com.example.community.dto.QuestionQueryDTO" resultMap="BaseResultMap">
    select * from QUESTION
    <where>
      <if test="search != null">
        and title regexp #{search}
      </if>
    </where>
    order by get_create desc limit #{offset}, #{size}
  </select>
  <select id="topicList" resultMap="BaseResultMap">
    select * from QUESTION order by comment_count desc, like_count desc, view_count desc
    limit #{offset}, #{size}
  </select>
  <update id="showView" parameterType="com.example.community.model.Question">
    update QUESTION
    set NEW_VIEW = #{newView}
    where id = #{id}
  </update>
</mapper>